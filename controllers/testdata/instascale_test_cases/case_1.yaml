apiVersion: codeflare.codeflare.dev/v1alpha1
kind: InstaScale
metadata:
  name: example
spec:
  controllerResources:
    limits:
      cpu: '2'
      memory: 2G
    requests:
      cpu: '2'
      memory: 2G
